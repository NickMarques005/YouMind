import { View, Text } from 'react-native'
import React from 'react'
import ProfileStack from '@navigation/stacks/app/both/profile/ProfileStack';

const Profile = () => {
    
    return <ProfileStack />
}

export default Profile;