import { useCallback } from "react"



const UseUserSocket = () => {

    const handleUpdateUserPersonalData = useCallback( () => {

    }, []);

    return { handleUpdateUserPersonalData }
}

export default UseUserSocket;